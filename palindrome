#include<stdio.h>
int main()
{
	int n,rev,rem,original;
	printf("enter the number : ");
	scanf("%d",&n);
	original=n;
	while(n!=0)
	{
		rem=n%10;
		rev=rev*10+rem;
		n/=10;
	}
	if(original==rev)
	{
		printf("it is palindrome...");
	}
	else
	{
		printf("not palindrome");
	}
	return 0;
}
